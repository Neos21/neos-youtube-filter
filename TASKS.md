# TASKS.md

本プロジェクトの実装タスクリスト。

- 実行ルール : このファイルの未完チェックリストは上から順に実行する。AI はファイル内で最初に出現する `- [ ]` (未完了のチェックリスト項目) を次タスクとして扱い、開発者の確認なしに別タスクに移行してはならない
- 実装ルールは [AGENTS.md](./AGENTS.md) を入口として `docs/agent-rules/` に分類している。詳細は `AGENTS.md` が指定する作業対象別のルールを参照する
- 完了した項目には `- [x]` を付ける
- 編集対象ファイルなどの補足情報は必要に応じて各フェーズの中に追記する


## タスクリスト

実現したい内容の正は [README.md のコンセプト](./README.md#コンセプト) とする。以下は実装順と完了条件であり、未実装の仕様を現行仕様として扱わない。

### 再開時・完了時の扱い

- 各チェック項目を1タスクとして扱う。配下の箇条書きはそのタスクの対象・完了条件であり、独立した次タスクではない
- 再開時は作業ルール、最初の未完タスク、依存する完了済みタスクの記録、現在のコード・差分を確認する。記載したパスは着手時点の実装に合わせて確認する
- 着手時に責務・対象ファイル・API・UI State・検証方法を必要に応じて具体化し、開発者の開始確認を取る。1回でレビューしきれない場合は、そのタスクを実行順を維持して分割する
- 完了時は当該項目の配下に、変更箇所、確定した契約や仕様の参照先、検証結果、残件を簡潔に残す。途中で中断した場合は未完のまま、実施済み範囲と次の作業を記録する
- 共通の検証は [workflow.md](./docs/agent-rules/workflow.md) に従う。以下の確認例は各タスク固有の観点とする。ユニットテストの追加は開発者から明示的に指示された場合だけ行う
- DB 操作・本番デプロイ・シークレット登録は [CONTRIBUTING.md](./CONTRIBUTING.md) に従い開発者が手動で行う。必要な操作が未実施の場合は依存する検証を完了扱いにせず、その項目で待機する

### 1. 仕様とデータ契約

- [x] 01. コンセプトの未確定事項と各レイヤーの責務を整理し、実装契約を開発者と確定する
    - 参照 : `README.md`、`ARCHITECTURE.md`、`server/routes/api/login/login.ts`、`client/helpers/admin-api.ts`、`scripts/ytf.js`、`migrations/`
    - 管理 SPA・メインスクリプトとも固定 Bearer トークンを使用し、JWT は一切使用しない。認証契約は以下の記録を参照する
    - 動画 ID、チャンネル ID・ハンドルの正規化、片方だけの登録、両方判明した際の紐付け・重複・競合、タイトルの任意性を決める。チャンネル名だけで同一チャンネルと判定しない
    - 文字列一致の対象・大小文字の扱い、正規表現の入力形式、フラグ省略時の `iu` と明示的な空フラグの区別、不正な正規表現・フラグの扱いを決める
    - 購読チャンネルによる非表示の適用範囲、複数条件に該当した場合の扱い、対象外ページ・Shorts 等の扱いを確定する。購読情報はブロック情報と別管理し、自動同期など未要求の機能を前提にしない
    - CRUD とフィルター情報取得 API のパス・メソッド・認証・入出力・エラー、キャッシュ更新タイミング、トグル OFF 中に非表示ボタンを押した場合の画面反映、トークンの設定方法を決める
    - 完了条件 : 決定内容と未対応範囲をこの項目の記録または適切な ADR に残し、以降のタスクから参照できる。実装前の計画を現行仕様文書へ混在させない
    - 完了記録 (2026-09-22) : 現在のログイン・Schema・Repository サンプル・メインスクリプト・作業ルールを確認し、開発者の回答と以下の実装契約を整理した。ここに記す内容は後続タスクで実装する契約であり、現行実装の説明ではない
    - 開発者が確定した仕様 : 両クライアントとも固定トークンを使用し、JWT は廃止する。購読チャンネルもトグル ON 時に非表示とし、検索結果への遷移時は OFF に戻す
    - 開発者が確定した一致条件 : 文字列は動画名・チャンネル名それぞれの `toLowerCase().includes(pattern.toLowerCase())` 相当で部分一致させる。正規表現はスラッシュで囲まず保存する。いずれかの非表示条件に一致すれば対象とし、購読情報を表示優先の例外にしない
    - フラグの保存契約 : `flags` は `TEXT NOT NULL DEFAULT 'iu'` とし、正規表現のフラグなしは空文字で保存する。管理画面の正規表現作成時は `iu` を初期入力する。API の作成時は未指定なら `iu`、明示した空文字はそのまま保持し、NULL は拒否する。文字列型ではフラグを使用せず空文字で保存する。パターンの PATCH は `type`・`pattern`・`flags` をすべて必須とし、文字列から正規表現への切替時もフラグを明示する
    - 開発者が確定した表示制御 : トグル OFF 中の登録成功時もカードを表示したままにする。メモリとキャッシュの条件は更新し、ON にした際に反映する。表示・非表示は常にトグル状態に従う
    - 開発者が確定した Shorts 対応 : ホーム・関連動画・検索結果内の Shorts カードも対象にする。`/shorts/動画ID` 等から動画 ID を抽出し、DOM からチャンネル識別子が確認できる場合はチャンネル単位でも扱う。Shorts 専用プレイヤーでは非表示・ボタン追加等を動かさない。別の拡張機能による通常動画ページへのリダイレクトは本プロジェクトでは実装しない
    - 認証の実装契約 : 共通の環境変数 `API_TOKEN` と Authorization の `Bearer` 値を照合する。両クライアントから同じ CRUD・情報取得 API を利用できる。`POST /api/login` は Bearer トークンの有効性確認として残し、成功時は `{ result: true }` を返す。管理画面は入力したトークンを確認成功後に既存 Store へ保存する。JWT の生成・検証・専用シークレット・パスワード照合は削除し、既存 JWT は再ログインで置き換える
    - トークン設定の実装契約 : 管理画面はトークン入力、メインスクリプトは初回入力と YouTube 側 LocalStorage への保存を基本とし、401 時に再設定できるようにする。公開される `/ytf.js` や URL にトークンを埋め込まない。認証設定不足は 500、未指定・不一致は 401、OPTIONS は認証を要求しない
    - データ契約 : 各テーブルは整数の `id` を主キーにする。`video_id` は一意かつ登録後は変更不可、チャンネルの `handle`・`channel_id` はそれぞれ NULL 許容かつテーブル内で一意とし、最低どちらか1つを必須とする。`title` は参考情報として NULL 許容とする。購読とブロックのテーブル間で一意性を共有しない
    - 識別子の入力契約 : 保存値は動画 ID・チャンネル ID・先頭 `@` 付きハンドルとし、管理画面では対応する YouTube URL からも抽出できるようにする。識別子の前後の空白を除去し、動画 ID・チャンネル ID の大小文字を保持する。ハンドルは先頭 `@` を補い、大小文字を同一視する照合用の小文字表現で保存する。URL のハンドルはパスをデコードする。取得不能な識別子は NULL とし、チャンネル名から推測しない
    - 登録・更新の契約 : POST は INSERT、PATCH は UPDATE、PUT は INSERT ... ON CONFLICT DO UPDATE を実行し、それぞれ RETURNING で全カラムを1クエリで返す。PATCH・PUT の更新句はカラム数にかかわらず buildUpdateQuery で構築する。PUT は動画を video_id、チャンネルを handle または channel_id の一意制約で照合し、省略項目は保持、明示 null はクリアする。id・created_at は更新しない。事前 SELECT・batch・割り込み対策は行わない
    - パターンの入力契約 : パターンは空文字を拒否し、意味を変える前後空白の除去をしない。正規表現とフラグの組み合わせを検証し、不正値は 400 とする。正規表現の連続評価では `lastIndex` をリセットする。フラグの対応範囲は対象ブラウザでの検証時に確認し、実行環境で構築できないキャッシュ内パターンはスキップしてエラーを通知する
    - API 契約 : `/api/blocked-videos`・`/api/blocked-channels`・`/api/blocked-patterns`・`/api/subscribed-channels` に GET (一覧)・POST (登録)、各 `/:id` に GET・PATCH・DELETE を用意する。JSON のカラム名は DB と同じ snake_case とする。一覧は id 昇順の配列、取得・登録・更新は保存後のレコード、削除は true を `result` に格納する。POST 成功は 201、PUT は新規・更新とも 200、取得・更新・削除は 200。不正入力は 400、存在しない ID は 404、DB の登録・更新失敗は 500 とし、エラーは `{ error: メッセージ }` とする 動画・チャンネルは各コレクションパスに PUT を追加する
    - 更新契約 : 動画・チャンネルの PATCH の未指定は保持、NULL 許容項目への明示 NULL はクリアとする。動画・チャンネルの空 PATCH は 400、存在しない ID は 404 とする。チャンネルの識別子が両方空になる更新や重複は DB 制約で拒否し、Controller が SQL エラーのメッセージをそのまま error に入れて 500 で返す。主キー・登録日時・未知の入力項目は Schema で拒否する
    - 取得 API 契約 : `GET /api/filter-rules` は `{ result: { blocked_videos: [...], blocked_channels: [...], blocked_patterns: [...], subscribed_channels: [...] } }` を返す。各配列は該当テーブルのレコードとし、取得失敗を空配列として扱わない
    - キャッシュ契約 : 保存形式に取得日時と条件を持ち、version・apiUrl は保存せず、起動時は有効な形式のキャッシュで表示を開始して API から再取得する。対象ページへの遷移時も再取得し、登録成功時はメモリとキャッシュへ即時反映する。通信失敗では既存キャッシュを利用し、401 は再設定を案内する。初回取得失敗・破損時は条件なしで表示を維持し、再試行できるようにする。定期ポーリングは初期実装に含めない
    - 責務と State : Controller は認証・入出力・Schema 検証・SQL エラー処理、Repository は SQL の実行、shared は型・Schema・共有処理を担当する。Service クラスは作らない。管理画面は認証を既存 Store、一覧・編集中入力・通信状態を必要な所有箇所で保持する。メインスクリプトは `window.__YTF__` 配下に初期化状態・条件・トグル・監視解除処理を保持する
    - 検証・引継ぎ : 今回は契約とタスクリストのみ変更し、実装・D1 操作は行わない。次は 02 の SQL 作成と開発者によるローカル適用。開発者から既存テーブル・保持すべき既存データともになしと確認済み
- [x] 02. 4テーブルの D1 スキーマと開発者向けの適用手順を用意する
    - 対象 : `migrations/`、必要に応じて `CONTRIBUTING.md`
    - `blocked_videos` に `video_id`・`title`、`blocked_channels` と `subscribed_channels` に `handle`・`channel_id`・`title`、`blocked_patterns` に `type`・`pattern`・`flags` を定義する
    - CRUD 用の識別子、NULL・既定値、動画 ID とチャンネル識別子の一意性、チャンネル識別子が最低1つ必要な制約、検索用インデックスを 01 の契約に合わせる
    - 現在の SQL は `examples` のサンプルなので、既存 DB の状態を確認せず破壊的な置換を前提にしない。作成・削除用 SQL の対応と適用順を整理する
    - 完了条件 : SQL と手順をレビュー可能にし、開発者によるローカル適用結果を記録する。AI は D1 の作成・SQL 実行・マイグレーションを行わない
    - 進捗 : 開発者より既存テーブル・保持対象データなしと確認。`migrations/create-tables.sql`・`drop-tables.sql` を4テーブルへ置き換えた。一意制約によるインデックスを利用し、重複インデックスは追加しない
    - 検証 : 差分チェック・Lint・ビルド成功。D1 とは独立した一時的なメモリ上の SQLite で、作成・削除、識別子の一意性・NULL 許容・最低1つ必須、パターン種別・空パターン拒否、フラグの既定値・空文字・NULL 拒否を確認した。D1 への適用は開発者が実施し、テーブル作成完了の報告を受けた
    - 完了記録 : 開発者の確認により 02 を完了とする。各テーブルへの `created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP` 追加後も開発者が DROP → CREATE による再作成を完了した。CONTRIBUTING への追記は開発者が削除済みのため復元しない。テーブル設計の詳細文書は配置先を含めて後回しとし、23 の文書整備時に検討する。次は 03 の共有型・Schema・正規化処理
- [x] 03. 4リソースの共有型・入力 Schema・正規化処理を実装する
    - 対象 : `shared/types/`、`shared/schemas/`、必要な `shared/helpers/`・`shared/services/`
    - Entity と作成・更新入力、部分更新時の未指定とクリアの違い、API の応答契約を定義する
    - 識別子の入力検証・正規化、チャンネル識別子が両方空になる更新の拒否、正規表現とフラグの検証を用意する
    - 完了条件 : 01・02 と整合し、正常入力・不正入力・片方だけのチャンネル識別子・フラグ省略と空指定の結果を確認する
    - 完了記録 : `shared/types/entities/` に4テーブルの型 (DB が生成する `created_at` を含む)、`shared/types/app/` にフィルター情報と API 応答の型、`shared/schemas/` に作成・更新 Schema、`shared/services/normalize-youtube-identifier.ts` に URL・識別子の正規化を追加した
    - 更新時の引継ぎ : 動画・非表示チャンネルは PATCH の部分 Schema で入力を検証し、DB 制約で更新後の整合性を保証する。resolveBlockedChannelUpdate は廃止した。未着手の購読チャンネル・パターンの更新処理も、着手時に最新の Repository・Controller 方針へ合わせる。id・created_at・未知の項目は作成・更新入力で拒否する
    - 検証 : 差分チェック・Lint・型チェックを含むビルド成功。一時的な実行で通常動画・Shorts・短縮 URL・チャンネル URL・ハンドルの正規化、異なるホストと不正入力の拒否、PATCH の省略・NULL・空文字、正規表現の既定フラグ・明示空文字・不正値・種別切替を確認した。テストファイルの追加や D1 操作は行っていない

### 2. API

- [x] 04. 固定 Bearer トークン認証と API の CORS・認証境界を実装する
    - 対象 : `server/routes/api/api.ts`、認証処理、`server/types/hono-bindings.ts`、`.dev.vars.example`、`server/routes/api/login/`、`shared/schemas/login-schema.ts`、管理画面のログイン・Store・API ヘルパー、認証関連文書
    - 01 の契約に沿って全 API を固定トークン認証に統一し、JWT 関連処理・設定を削除する。秘密値は環境変数から読む。配信する `/ytf.js` やリポジトリに固定トークンを埋め込まない
    - `www.youtube.com`・`m.youtube.com` からの Authorization 付きリクエストと OPTIONS を扱い、更新に PATCH を使う場合は現在の CORS 許可メソッドへ追加する
    - 完了条件 : トークンなし・不正・正常・サーバ設定不足の応答、認証前のプリフライト、管理画面のログイン経路を確認する。公開スクリプト取得と保護対象 API を区別する
    - 完了記録 : API 共通 Middleware で `API_TOKEN` と Bearer トークンを照合し、CORS を認証より先に適用した。`POST /api/login` は `{ result: true }` を返す確認用 API とし、管理画面は入力した固定トークンを保存する。JWT 発行処理・旧 Binding・サンプルの JWT 認証を削除し、既存 Store の保存形式を更新して旧認証情報をクリアする
    - 検証 : Lint・型チェックを含むビルド成功。Hono のリクエスト実行で未指定・不一致・正常トークン・設定不足・両 YouTube ドメインの OPTIONS とエラー応答の CORS を確認した。クライアントは模擬 LocalStorage・通信で旧情報の破棄、固定トークン保存・復元、Authorization 付与、401 時のログアウトを確認した
    - 手動確認 : 開発者が `.dev.vars` に `API_TOKEN` を設定して開発サーバを再起動し、同じ値で画面ログインを確認する。実ブラウザでの操作と本番シークレット登録・デプロイは未実施。詳細文書の追加は後回しとし、既存の認証説明と設定例のみ更新した
- [x] 05. 非表示動画の Repository と CRUD・UPSERT API を実装する
    - 対象 : `server/repositories/blocked-videos-repository.ts`、`server/routes/api/blocked-videos/`、`shared/schemas/blocked-video-schema.ts`
    - 完了記録 : GET 一覧・1件、POST、PATCH、PUT、DELETE を実装済み。POST は INSERT のみ、PATCH は title だけを UPDATE、PUT は video_id を照合キーに UPSERT する。書き込みは各1クエリの RETURNING で全カラムを返す
    - レビュー反映 : Repository は SQL 実行と結果返却のみとし、例外は Controller の try/catch でエラーレスポンスに変換する。Repository の Result 型と事前 SELECT を廃止し、更新句は常に buildUpdateQuery を使う。video_id は登録後変更不可とし、PATCH に含まれた場合は 400 を返す
    - 検証 : Lint・ビルド成功。一時 SQLite を使った Hono リクエスト実行で全 CRUD・PUT、各書き込みのクエリ数が1回であること、全カラムの返却、重複 INSERT の失敗、省略と null、登録日時保持、認証・404・SQL エラー返却を確認した。実 D1 の操作は行っていない
- [x] 06. 非表示チャンネルの Repository と CRUD・UPSERT API を実装する
    - 対象 : `server/repositories/blocked-channels-repository.ts`、`server/routes/api/blocked-channels/`、`shared/schemas/blocked-channel-schema.ts`
    - 完了記録 : GET 一覧・1件、POST、PATCH、PUT、DELETE を実装済み。ハンドルだけ・チャンネル ID だけ・両方で登録できる。PUT は handle または channel_id の一意制約で照合し、省略項目は保持、明示 null はクリアする
    - レビュー反映 : Service と resolveBlockedChannelUpdate を削除した。Repository は INSERT・UPDATE・UPSERT を各1クエリの RETURNING で実行し、PATCH・PUT の更新句は buildUpdateQuery で構築する。事前 SELECT・batch・割り込み対策・onError は使わず、Controller が SQL エラーをそのまま error メッセージとして返す
    - 検証 : Lint・ビルド成功。一時 SQLite で CRUD・PUT、各書き込みの1クエリ完結、全カラムの返却、省略項目保持、null クリア、登録日時保持、別レコードへ一致する識別子の競合、識別子の全消去時の CHECK エラー、認証・404・SQL エラー返却を確認した。D1 操作・スキーマ変更は不要
- [x] 07. 非表示パターンの Repository と CRUD API を実装する
    - 対象 : `server/repositories/`、`server/routes/api/`
    - `blocked_patterns` の文字列・正規表現・フラグを保存し、作成時と更新後の組み合わせを検証する
    - 完了条件 : CRUD、型の切替、既定フラグ `iu`、明示フラグ、不正パターン・フラグの拒否を確認する
    - 完了記録 (2026-09-22) : `BlockedPatternsRepository` と `/api/blocked-patterns` の GET・POST・PATCH・DELETE を実装した。既存 Controller のコメント・例外処理の書き方に合わせ、Repository は SQL 実行のみとした
    - 確定した更新契約 : PATCH は `type`・`pattern`・`flags` をすべて必須とする。共有 Schema で更新後の組み合わせを検証し、事前 SELECT・既存値との統合を行わず、buildUpdateQuery と UPDATE ... RETURNING の1クエリで全カラムを返す。POST も INSERT ... RETURNING の1クエリとする
    - 検証結果 : Lint・ビルド成功。メモリ上の SQLite を使った28件の API 確認で CRUD、型の切替、既定・空フラグ、空白保持、不正入力・部分 PATCH の拒否、404、認証、登録日時の保持、書き込み時の単一クエリを確認した。D1 操作・マイグレーションは不要
- [x] 08. 購読チャンネルの Repository と CRUD API を実装する
    - 対象 : `server/repositories/`、`server/routes/api/`
    - `subscribed_channels` をブロックとは別リソースとして管理し、06 で確定した識別子の正規化・紐付け規則を再利用する
    - 完了条件 : CRUD、識別子補完、重複・競合を確認し、同じチャンネルのブロック情報を変更しない
    - 完了記録 (2026-09-22) : `SubscribedChannelsRepository` と `/api/subscribed-channels` の GET・POST・PATCH・PUT・DELETE を実装した。非表示チャンネルと同じ正規化・更新方式を採用し、購読用 Schema は同テーブルのファイル内に独立して定義した
    - 更新契約 : POST は INSERT、PATCH は UPDATE、PUT は一意制約による UPSERT とし、各書き込みは RETURNING 付きの1クエリで全カラムを返す。PATCH・PUT の更新句は buildUpdateQuery で構築する。省略項目は保持、明示 null はクリアし、空 PATCH は拒否する。識別子消失・競合は DB 制約に任せ、事前 SELECT と既存値統合用 Resolver は使用しない
    - 検証結果 : Lint・ビルド成功。メモリ上の SQLite による33件の API 確認で CRUD・UPSERT、URL 正規化、識別子補完、保持・クリア、重複・競合、不正入力、404・認証、登録日時の保持、単一クエリ、ブロック情報の独立性を確認した。SQL 例外の応答は既存 Controller と同じ Hono 標準の 500 に従う。D1 操作・マイグレーションは不要
- [x] 09. メインスクリプト向けフィルター情報取得 API を実装する
    - 対象 : `server/repositories/`、`server/routes/api/`、応答用の共有型
    - 4テーブルをまとめる Read Model を固定 Bearer トークンで取得できるようにする。Service クラスを作らない最新方針に従い、集約処理の配置は着手時に具体化する
    - メインスクリプトが必要とする識別子・パターン・フラグ等を返し、取得失敗を正常な空リストとして返さない
    - 完了条件 : 4種類が揃った場合・全件空・DB エラー・認証エラーを確認し、応答契約を記録する
    - 完了記録 (2026-09-22) : `GET /api/filter-rules` を実装した。Controller で既存4 Repository の findAll を並列に呼び、既存の共有型 FilterRules にまとめて返す。Service・SQL・共有型の追加は不要
    - 応答契約 : `{ result: { blocked_videos, blocked_channels, blocked_patterns, subscribed_channels } }` を 200 で返す。各配列は全カラムを含む id 昇順のレコードとし、登録なしは空配列とする。いずれかの取得失敗時は部分結果や空配列を返さず、既存 Controller と同じ Hono 標準の 500 応答に従う。共通の固定 Bearer 認証・CORS を適用する
    - 検証結果 : Lint・ビルド成功。メモリ上の SQLite と取得失敗の模擬を使った12ケースで、全件空・4種類の全カラムと並び順・各テーブルの取得失敗・認証不備・認証設定不足・YouTube 両ドメインの CORS を確認した。D1 操作・マイグレーションは不要

### 3. 管理 SPA

管理画面の共通方針 : 各ページの `text-right` 内に「新規登録」ボタン、その下に一覧表を置く。初期表示時と追加・更新・削除の成功後に全件再取得する。1つのページコンポーネント内に一覧と daisyUI の `.modal` を実装し、新規登録と編集のフォームモーダルは共通化せず別々に管理する。新規登録には入力可能な全項目、編集には変更可能な全項目と変更不可項目の表示を用意する。`.modal-action.justify-between` 内は、新規登録では左に「キャンセル」・右に「追加する」、編集では「削除する」「キャンセル」「更新する」を配置する。コンポーネントの切り出し・共通コンポーネント化は行わない

- [x] 10. 非表示動画の管理画面と管理メニューを実装する
    - 対象 : `client/routes.ts`、`client/layouts/admin-layout.tsx`、`client/pages/`、`client/helpers/admin-api.ts`
    - 04 で固定トークンへ移行した認証と既存レイアウトを利用し、非表示動画の一覧・追加・編集・削除を用意する。確定した入力形式で動画 ID と参考タイトルを扱う
    - 一覧データ、編集中入力、読込中・保存中・エラーを整理し、導出できる値を重複 State として保持しない
    - 完了条件 : CRUD の反映、入力エラー、API 失敗時の入力保持と再試行、401 時の既存ログアウト動作を確認する
    - 実装済み (2026-09-22) : `/blocked-videos` と管理メニューを追加した。ページ内に一覧・新規登録・編集用の別々のモーダルを実装した。新規登録は動画 ID または URL・参考タイトルを入力し、編集はタイトルだけを送信する。ID・動画 ID・登録日時は表示専用とした。初期表示と各操作成功後に全件再取得し、保存失敗時は入力を保持、一覧取得失敗時は既存一覧を保持して再取得ボタンを表示する
    - 検証 : Lint・ビルド成功。ブラウザ検証は実行環境の `libnspr4.so` 不足で Chromium が起動せず未実施。次回はブラウザで CRUD と各操作後の一覧再取得、動画 URL の正規化、不正入力、API 失敗時の入力保持・再試行、保存成功後の再取得失敗、モバイル表示、401 時のログアウトを確認する。その後、開発者から全体的な機能 OK と次タスク着手の承認を受け、完了扱いとした。DB マイグレーションは不要
- [x] 11. 非表示チャンネルの管理画面を実装する
    - 対象 : `client/pages/`、ルート・管理メニュー
    - ハンドル・チャンネル ID・参考チャンネル名の一覧・追加・編集・削除を用意し、既知の識別子を再入力せず編集できるようにする
    - 完了条件 : 片方だけの登録、両方への補完、重複・競合エラーの表示と再試行を確認する
    - 実装済み : `/blocked-channels` と管理メニューを追加した。開発者が調整した非表示動画ページに State・コメント・イベント処理・テーブル・フォームの書き方を合わせた。ハンドル・チャンネル ID・チャンネル名を新規登録・編集でき、編集時は既存値を引き継ぎ、ID・登録日時は表示専用とした。成功後の全件再取得と、失敗時の入力保持・再試行は動画ページと同じ方式とした
    - 検証 : Lint・ビルド成功。フォームが共有 Schema に渡す入力について6件を確認し、片方のみの登録、URL 正規化、不正入力の拒否、識別子補完、空欄の null 化を確認した。ブラウザの依存ライブラリ不足は前タスクから未解消のため、実画面での CRUD・重複/競合エラー表示・入力保持と再試行は未確認。その後、開発者が2画面を調整し、次タスクへの移行を承認したため完了扱いとした。DB マイグレーションは不要
- [x] 12. 非表示パターンの管理画面を実装する
    - 対象 : `client/pages/`、ルート・管理メニュー
    - 文字列・正規表現の種別、パターン、フラグを CRUD できるようにし、既定 `iu` と明示指定の関係が分かる入力にする
    - 完了条件 : 種別変更、フラグの保持・クリア、不正な正規表現の表示、保存失敗後の修正を確認する
    - 実装済み : `/blocked-patterns` と管理メニューを追加した。調整済みの2画面に合わせ、一覧・新規登録・編集用の別々のモーダルを1ページ内に実装した。種別・パターン・フラグを入力し、編集時は ID・登録日時を表示専用とする。PATCH は3項目をすべて送る。保存成功後は全件再取得し、失敗時は入力を保持する
    - フラグの扱い : 新規登録時と保存済み文字列型の編集開始時は、正規表現に切り替えた際の入力値として iu を用意する。正規表現型の編集では保存値をそのまま使用し、空文字も保持する。モーダル内の種別切替ではフラグ入力を保持し、文字列型の保存時のみ共有 Schema で空文字に揃える。パターンの前後空白は保持し、正規表現はスラッシュで囲まない旨を表示する
    - 検証 : Lint・ビルド・差分チェック成功。共有 Schema への入力18件で文字列/正規表現、空白保持、既定/明示/空フラグ、不正な正規表現・フラグ、PATCH 必須項目を確認した。実ブラウザの操作確認は既知の依存ライブラリ不足により未実施。その後、開発者がパターン入力を単一行に変更し、input に type="text" を明記したうえで、続きの実装を承認したため完了扱いとした。DB マイグレーションは不要
- [x] 13. 購読チャンネルの管理画面を実装する
    - 対象 : `client/pages/`、ルート・管理メニュー
    - ハンドル・チャンネル ID・参考チャンネル名を CRUD できるようにし、非表示チャンネルとは別の管理画面・データとして扱う
    - 完了条件 : CRUD と識別子補完、同じチャンネルが両リストにある場合の独立した編集・削除を確認する
    - 実装済み : `/subscribed-channels` と管理メニューを追加した。調整済みの非表示チャンネルページに合わせ、一覧・新規登録・編集用の別々のモーダルを1ページ内に実装した。全テキスト入力に type="text" を明記し、識別子・チャンネル名の入力保持、URL 正規化、成功後の全件再取得、失敗時の再試行を同じ方式で実装した。購読用 API・Schema・Entity のみを使用する
    - 検証 : Lint・ビルド成功。共有 Schema とメモリ上の SQLite による API 確認で、フォーム入力の正規化・不正入力の拒否、識別子の補完・クリア、CRUD、同じチャンネルのブロック情報が購読情報の更新・削除で変わらないことを確認した。実ブラウザでの操作確認は既知の依存ライブラリ不足により未実施。その後、開発者から OK と次タスク着手の承認を受け、完了扱いとした。DB マイグレーションは不要

### 4. YouTube 上で動くメインスクリプト

- [x] 14. メインスクリプトを TypeScript 化し、esbuild による独立ビルドと `/ytf.js` の配信を実装する
    - 対象 : `scripts/ytf.js` → `scripts/ytf.ts`、`scripts/tsconfig.json`、ルートの `tsconfig.json`、`package.json`・ロックファイル、必要な ESLint 設定、`public/_headers`、ビルド手順
    - メインスクリプトは TypeScript で実装し、`scripts/ytf.ts` を入口として必要に応じてモジュールに分割する。React・Hono・Vite とは独立したビルドとする
    - esbuild を直接 devDependencies に追加し、Bundle・Minify を行う。`platform=browser`・`format=iife` で、外部モジュールの読込を必要としない単一の `build/client/ytf.js` を生成する。`target` は対応する iPhone Safari 等のバージョンに合わせて決める
    - esbuild は型チェックを行わないため、DOM の型を使用する専用の `scripts/tsconfig.json` と `tsc --noEmit` で検証する。React Router・Workers・Vite の型生成に依存させず、ルート tsconfig の対象範囲も整理する
    - `npm run build:ytf` でメインスクリプトの型チェックと Bundle・Minify を単独実行できるようにする。`npm run build` は既存のアプリビルド完了後に `build:ytf` を実行し、出力先の清掃による成果物の消失を防ぐ
    - 完了条件 : アプリの型生成・ビルド前でも単独ビルドが成功し、全体ビルド後も `/ytf.js` が JavaScript として取得できる。型エラー時はコマンドが失敗し、生成物に秘密値を含まず、既存の SPA・API 配信を維持する
    - 完了記録 : `scripts/ytf.ts` と独立した `scripts/tsconfig.json` を追加し、esbuild 0.28.1 を直接依存として固定した。build:ytf は型チェック後に browser/IIFE 形式で Bundle・Minify し、build/client/ytf.js を生成する。開発者承認により対象は Safari 16・Chrome 110 とした
    - ビルド契約 : build・build-only の最後に build:ytf を実行する。ルート tsconfig は scripts を除外し、導入用 JavaScript 例は ESLint の型情報を使用しない。既存 npm 設定が package-lock=false でロックファイルも未管理のため、新規ロックファイルは追加していない。実行手順は CONTRIBUTING.md を参照する
    - 検証結果 : Lint・全体ビルド成功。アプリ・生成型・シークレットなしの一時環境で単独ビルド成功、型エラー時の停止、圧縮された通常スクリプトとしての構文を確認した。ローカル Wrangler で /ytf.js の本体・JavaScript MIME・YouTube 両ドメイン向け CORS、SPA フォールバック、未認証 API の 401 を確認した。D1 クエリ・デプロイは未実行
    - 次タスクへの引継ぎ : スクリプト本体は既存の読込確認用 alert と /api/test 呼出を TypeScript に移した段階。実際の起動・設定は15、フィルター情報取得は16以降で実装する。Safari・Chrome 実機での動作は未検証
- [x] 15. メインスクリプトの起動・設定・重複ロード防止を実装する
    - 対象 : `scripts/ytf.ts` と必要な TypeScript モジュール・型定義、導入手順
    - 14 のビルド基盤を利用し、Tampermonkey・ブックマークレットから同じ `/ytf.js` を実行する
    - `window.__YTF__` で初期化中も含めて重複実行を防ぎ、起動失敗後の再試行方法を定める。API の URL とユーザー自身のトークンを安全に設定できる入口を用意する
    - 完了条件 : ビルド成果物が JavaScript として取得でき、秘密値を含まない。Tampermonkey・ブックマークレットの双方で設定を渡せ、再実行で UI・イベントが重複しない
    - 完了記録 : eval・@require による評価時に自動起動し、window.__YTF__.status で初期化中・起動済みの多重実行を防ぐ。start・configure や外部設定引数は使用しない。API URL は scripts/ytf.ts 内の定数とする
    - トークン契約 : 固定キー ytf:token で LocalStorage に保存し、API URL を変更しても保存場所は変えない。未保存・空欄時に prompt で入力を求め、POST /api/login 成功後に保存する。401 は一致する保存値を削除し、キャンセル・通信失敗後も再評価で再試行できる。保存不可でも起動を継続する
    - 導入契約 : bookmarklet-example.js は開発者指定により元の内容に戻した。Tampermonkey は @require のみで起動する。手順は docs/features/main-script.md に記載し、公開コード・URL・ログにはトークンを含めない
    - 検証 : Lint・全体ビルドと模擬環境で自動起動・重複評価・固定キー保存・401 後の再入力・キャンセル後の再試行を確認した。実ブラウザ・実トークンの通信は未検証。情報取得とキャッシュは16以降で実装する
- [x] 16. フィルター情報の取得と LocalStorage キャッシュを実装する
    - 対象 : メインスクリプトの API クライアント・キャッシュ処理
    - 09 の API を Authorization ヘッダ付きで呼び、正常な取得結果を LocalStorage に保存する。キャッシュの形式・版・更新タイミングは 01 の契約に従う
    - 通信失敗、401、キャッシュ破損・形式変更、ストレージ利用不可、初回キャッシュなしを区別する。認証失敗を隠さず、取得失敗で正常キャッシュを空データに置き換えない
    - 完了条件 : 新規取得・キャッシュ利用・再取得・失敗時の動作を確認する。`www.youtube.com` と `m.youtube.com` では保存領域が異なることを考慮する
    - 完了記録 : 起動時にキャッシュを復元し、Bearer 付き GET /api/filter-rules で再取得する。保存形式は version: 1・fetchedAt・apiUrl・rules とし、固定キー ytf:filter-rules に保存する。トークンは従来どおり ytf:token に保存する。ログイン確認専用リクエストは条件取得で置き換えた
    - 失敗時の契約 : 通信失敗・HTTP エラー・不正応答でもメモリとキャッシュの正常な条件を維持する。初回未取得は null、正常な空リストは4配列で区別する。401 は unauthorized とし、一致する保存トークンのみ削除する。破損・版違い・取得元違い・保存領域使用不可は通知し、正常に取得できた条件はメモリで継続使用する
    - 後続処理への入口 : window.__YTF__ に rules・fetchedAt・error・refresh・updateRules を追加した。対象ページの yt-navigate-finish で再取得し、登録成功後は取得完了を待って updateRules でメモリとキャッシュに反映する。DOM 抽出・非表示・トグルは後続タスクで実装する。既存のホスト判定が YouTube 上で早期終了していたため、対象2ドメインとの完全一致判定に修正した
    - 検証結果 : Lint・全体ビルド成功。生成 IIFE を模擬環境で実行した15ケースで、起動時キャッシュ復元、新規取得、通信失敗、401・500、不正応答、破損・版違い・取得元違い・不正日時、保存不可、再試行、登録結果反映、対象ページ遷移、重複取得、両ドメインの起動を確認した。実ブラウザ・実 API 通信は未検証
- [x] 17. PC・モバイルの対象ページから動画情報を抽出する処理を実装する
    - 対象 : メインスクリプトのページ判定・DOM 抽出処理
    - `new URL(location.href)` でホーム・動画ページ・検索結果・対象外を判定し、`www.youtube.com` と `m.youtube.com` の実際の DOM を確認する
    - 通常動画と Shorts の各カードから動画 ID・動画タイトル・チャンネル名・取得可能なハンドルまたはチャンネル ID・サムネイル・非表示にするカード要素を取得する
    - Shorts 専用プレイヤーは対象外とし、対象ページ内の Shorts カードでは `/shorts/動画ID` 等の URL から抽出する。チャンネル情報は DOM で確認できる範囲だけを利用する
    - 完了条件 : 両ドメインの対象3画面について通常動画・Shorts の DOM と抽出例を記録する。不明な識別子を捏造せず、関連動画以外の再生中動画・操作 UI を巻き込まない
    - 実装済み (2026-09-23) : scripts/dom/ にページ判定・セレクタ定義・抽出処理を分離し、window.__YTF__.page・getCards() を追加した。タイトル・チャンネル名・識別子・画像 URL・カード要素を取得し、入れ子の重複を除外する。表示制御や DOM 監視は追加していない。利用方法は docs/features/main-script.md を参照する
    - DOM 調査 : 一時配置したライブラリで Chromium を起動し、未ログインの www と m のホーム・検索結果 (Beatles)・動画ページ (dQw4w9WgXcQ) を確認した。PC は 1440×1000、モバイルは iPhone Safari の User-Agent と 390×844 のエミュレーションを使用した。iPhone 実機・Safari エンジンの検証ではない
    - PC の抽出結果 : 検索結果では ytd-video-renderer・yt-lockup-view-model の通常動画28件と、ytm-shorts-lockup-view-model-v2 内の Shorts 20件を抽出した。例は CGj85pVzRJs (The Beatles・チャンネル ID 取得可) と A2ixOpa2UeY (Shorts・識別子なし)。関連動画では ytd-watch-next-secondary-results-renderer 内の yt-lockup-view-model から通常動画19件を抽出した。例は vbK7SmzjK8g (STEREO NOSTALGIA・識別子なし)。コレクションのカードは除外した
    - モバイルの抽出結果 : 検索結果では ytm-video-with-context-renderer の通常動画16件と ytm-shorts-lockup-view-model の Shorts 12件を抽出した。例は uSo2dagQ8EE (@goodday-k6p2m) と A2ixOpa2UeY (動画タイトルとチャンネル名を別の位置から取得・識別子なし)。関連動画では section-identifier="related-items" 内の通常動画12件を抽出した。例は nMAcZp5Tpjw (@abcarco89)
    - 検証 : Lint・全体ビルド成功。取得した6画面の DOM と、一時的な26ケースで入れ子の重複・同じ動画の別カード・探索範囲・広告/コレクション除外・非表示の旧ページ・画像 URL からの Shorts ID 取得・識別子の正規化と欠落を確認した。生成 IIFE でも getCards の公開、DOM 再利用時の再取得、URL 変更、Shorts 専用ページで空配列となることを確認した。恒久的なテストファイル・依存パッケージは追加していない
    - 残件 : ホームは両ドメインとも「まずは検索してみましょう」と表示され、動画カードがなかった。ytd-browse[page-subtype="home"] 内の ytd-rich-grid-renderer と ytm-browse 内の ytm-rich-grid-renderer の存在は確認したが、通常動画・Shorts の実データ抽出は未確認。関連動画欄にも Shorts が出現しなかった。開発者より実機確認を非表示処理の実装後にまとめて行う方針を受けたため、17の実装は完了扱いとし、未確認の組み合わせは24の実機検証に引き継ぐ
- [x] 18. フィルター判定と CSS クラスによる非表示・復元を実装する
    - 対象 : メインスクリプトの照合処理・CSS 注入・カード更新
    - 動画 ID、チャンネルのハンドル・ID、動画名またはチャンネル名への文字列・正規表現、購読チャンネルによる条件を 01 の仕様どおり評価する
    - `.ytf-hidden { display: none !important; }` 相当を一度だけ注入し、クラスの付け外しで制御する。既存の `style` 属性を書き換えない
    - 正規表現は使い回し時の `g`・`y` の状態を考慮し、不正なキャッシュ内パターンで全体が停止しないようにする
    - 完了条件 : 各条件の一致・不一致・情報不足・複数条件の重複、連続した正規表現評価を確認し、条件が外れたカードを復元できる
    - 実装記録 (2026-09-23) : 開発者の「非表示処理まで含めて実装後に動作確認する」方針に従い、18・19と共通処理整理・デバッグ表示をまとめて実装した。scripts/filter/ に条件判定と非表示・復元処理を分離し、動画・非表示/購読チャンネル・文字列/正規表現のいずれかに一致したカードにだけ ytf-hidden を付ける。既存の style 属性は維持する
    - 共通処理 : scripts/helpers/ の LocalStorage 操作と API 呼び出しは Result 型を返す。条件の取得・保存状態は scripts/state/ に移し、ytf.ts は初期化と接続を担当する。命名は YouTube、配列型は Array<T>、コメント内のプログラム値はバッククォート表記に揃えた
    - キャッシュの更新契約 : 開発者の指示により version・apiUrl は保存しない。fetchedAt・rules のみを保存し、旧形式の追加項目は読み飛ばす。保存キーとトークン保存方法は継続する。これは16の版・取得元検証の契約を置き換える
    - 検証 : Chromium の模擬 DOM・API による結合検証で、4種類の条件、大小文字無視、正規表現の連続評価、不正な正規表現のスキップ、条件変更と OFF による復元、元の style 保持を確認した。保存・通信失敗、HTTP エラー、不正応答でも既存条件を維持する
- [x] 19. 右上の非表示トグルと YouTube 内のページ移動・追加読込への追従を実装する
    - 対象 : メインスクリプトの UI・イベント・DOM 監視
    - 右上にチェックボックスを配置し、ホーム・動画ページでは ON、検索結果では OFF をページ移動ごとの初期値にする。同じページ内の追加読込ではユーザーの操作を維持する
    - `yt-navigate-finish` で URL と初期状態を再評価し、DOM の遅延描画・無限スクロール・カードの再利用を監視して再判定する
    - 監視対象・処理頻度・解除方法を整理し、自身のクラスやボタン追加による監視ループとリスナー重複を防ぐ
    - 完了条件 : 対象3画面間の遷移、戻る・進む、追加カード、ON/OFF、対象外ページへの移動を確認する。OFF で本スクリプトが隠したカードを復元する
    - 実装記録 (2026-09-23) : 右上に非表示・ログのチェックボックスと再取得ボタンを追加した。ホーム/動画は ON、検索は OFF を初期値とし、同一 URL の再描画とハッシュ変更では操作状態を維持する。yt-navigate-finish・popstate と MutationObserver で追従し、短時間の変更を100ミリ秒単位でまとめる。自身の UI・非表示クラス更新を監視から除外し、destroy() で解除・復元できる
    - デバッグ : console.log・console.error に処理内容とエラー詳細を出す。ログ ON 時は左下のテキストエリアに同じ内容を表示し、ytf:debug に切替状態を保存する。直近200件・1件2000文字まで保持し、トークン・送信本文はログに含めない。実装は scripts/ui/、使い方は docs/features/main-script.md を参照する
    - 検証 : 模擬 DOM・API の29項目でページ遷移、追加描画、カード再利用、重複ロード、監視ループ回避、ログ表示・保存・件数上限、エラー後の再試行、トークンのログ出力抑止を確認した。追加で DOM 準備前の重複評価、取得済みモバイル実 DOM の Shorts 非表示/復元、解除後の再起動とログ設定復元を確認した。Lint・全体ビルド・差分チェック成功。実機での YouTube・Safari・Tampermonkey の確認は24に引き継ぐ
- [ ] 19-1. メインスクリプトを簡素化し、処理の流れと保守性を優先してリファクタリングする
    - 実行順 : 19の次、非表示ボタンを実装する20・21より先に行う。既存タスク番号と参照は維持する。この項目は開発者と合意した変更方針であり、実装完了までは現行仕様として扱わない。完了済みタスクの記録と異なる起動・認証・キャッシュ・照合・UI の契約は、この方針で置き換える
    - 対象 : `scripts/ytf.ts`、`scripts/helpers/`、`scripts/state/`、`scripts/filter/`、`scripts/dom/`、`scripts/ui/`、関連する型・Schema・機能文書。単にファイルを細分化せず、不要な責務・状態・公開 API・自動復旧処理を減らす。主要な流れを「起動」「条件の読込・保存」「カード処理」「操作メニュー」として追える構成にする
    - 起動順 : 対象サイトと重複起動の確認 → トークン読込 → キャッシュ読込 (なければ API 取得) → 操作メニュー配置 → カード処理と監視開始の順序がコードから読み取れるようにする。eval・Tampermonkey の @require による自動起動と、コード内定数の API URL は維持する
    - 認証の簡素化 : 起動時に LocalStorage からトークンを取得できなかった場合だけ、一度だけ prompt で入力を求める。キャンセル・空入力なら終了する。API が401を返したら動作を停止し、監視を解除して非表示を復元し、再読み込みを案内する。ページ遷移や通信失敗を契機に再入力を繰り返さず、自動的な再認証・再試行を行わない。正常なキャッシュ利用中はトークン確認の API も呼ばない
    - 状態の簡素化 : window.__YTF__ は主に重複起動防止に使用し、外部操作用の多数のプロパティや多段階の status を必要としない設計にする。トークンを条件 Store に抱え込ませず、条件取得と将来の PUT が同じ認証付き通信処理を利用できるようにする。大きな Store や汎用的な状態管理機構を新設しない
    - API 呼出頻度 : 有効なキャッシュがあれば起動時もページ遷移時も全件取得しない。キャッシュがない・読み取れない場合の初回取得と、利用者が明示する「再取得」だけで全件取得する。ポーリングは行わない。通信失敗時は既存条件を保持し、初回取得も失敗した場合は非表示にせず、明示的な再取得で対処する
    - 保存内容 : 非表示動画は動画 ID、非表示・購読チャンネルはハンドルとチャンネル ID、パターンは種別・文字列・フラグに絞る。DB の id・created_at・参考用 title、version・apiUrl は保存しない。取得日時も表示用途がなければ削除する。API の全カラム応答をそのまま保持せず判定用の軽量な形式に変換する。旧キャッシュへの対応は簡単な変換または破棄と初回取得に留め、複雑な移行処理を作らない
    - URL・エラー処理 : ホスト名は youtube.com を含むかの簡単な判定にし、www・m の列挙やプロトコルの検証など対象用途で不要な判定を減らす。PC・モバイルで探索範囲を変えるための判定は必要に応じて残す。requestApi は通信・JSON 読込失敗をその場で Result のエラーとして返し、throw・再スローを使わない
    - 照合の簡素化 : 非表示判定はカード単位で行い、動画 ID・チャンネル ID・ハンドルはカードの HTML またはリンク URL、キーワード・正規表現は textContent を照合対象とする。タイトル・チャンネル名だけに限定するための精緻な抽出を判定の前提にしない。textContent に含まれる説明文・視聴回数なども一致対象となることを仕様として受け入れる。自分で追加したボタンやログの文字列は照合対象から外す
    - 維持する挙動 : 文字列は大文字小文字を区別しない部分一致、正規表現は保存したフラグで評価し、g・y の評価位置をリセットする。不正な正規表現はログに残して当該条件だけを除外する。ホーム・動画ページは初期 ON、検索結果は初期 OFF、同じページ内では手動操作を維持する。Shorts 専用プレイヤーは対象外とし、個別カードの CSS クラスによる非表示・復元と、既存 style の保持を継続する
    - DOM 操作量 : 初回・ページ移動・条件変更時は対象カード全体を処理する。通常の MutationObserver 通知では、追加・変更されたカードを Set などにまとめて処理し、通知のたびに全カードを再走査しない。同じカードの HTML・テキスト取得を条件ごとに繰り返さない。自分の UI・ログ・非表示クラス変更を除外し、複雑な差分管理を増やさず監視ループを防ぐ
    - 操作 UI : 右上に小さな details 要素を配置し、開閉するメニュー内に非表示 ON/OFF・再取得・デバッグ切替・ログ用テキストエリアをまとめる。デバッグの ON/OFF 保存とログの件数・文字数上限は維持する。メニューが閉じている間やデバッグ OFF 中はログ欄を書き換えず、表示時に保持済みのログを反映する
    - ログ方針 : デプロイ後に開発者が取得したログから原因を追えるよう、起動手順、キャッシュの有無・読込/保存結果、API の開始・終了・HTTP ステータス・エラー詳細、ページ判定、監視で処理したカード数、非表示/復元の対象と一致理由、停止・解除を console.log・console.error に詳しく出す。画面ログでも同じ内容を確認・コピーできるようにする。トークン・Authorization ヘッダー・送信本文・カードの HTML 全体は出力せず、同じ情報の連続出力と画面更新は抑える。自動処理のエラー通知で alert を繰り返さず、ログと操作メニュー内の通知を利用する
    - 後続ボタンへの接続 : カード走査と監視をボタン専用に重複させない。配置やクリック時に必要な動画 ID・チャンネル識別子・参考タイトル・サムネイルを取得し、表示名から識別子を推測しない。20・21は「PUT → 保存結果を判定用条件とキャッシュに追加 → 再判定」の流れで実装できる構成にする。登録後の全件再取得は不要とし、OFF 中は登録成功後も隠さない。ボタン自体の実装は本タスクに含めない
    - 完了条件 : 起動順と責務が追えるコードになり、不要な状態・公開 API・分岐が削減されている。初回のみのトークン入力、キャッシュ利用時・ページ移動時に API が呼ばれないこと、明示的な再取得、401での停止・復元、軽量な保存形式、カード単位の追加処理、details の操作とログ出力を確認する。Lint・ビルド・差分チェックを実施し、必要な一時検証を行う。ユニットテストの恒久追加は行わない
    - 実機検証の順序 : リファクタリング完了後に20・21のボタンを実装する。ひととおり機能が揃ってから開発者がデプロイ・動作確認し、問題があればログをもとに調査する。AI は本番デプロイを実行せず、実機未確認は24の残件として保持する
- [ ] 20. サムネイル上の「この動画を非表示にする」ボタンを実装する
    - 対象 : メインスクリプトのカード操作 UI・登録 API 呼び出し・キャッシュ更新
    - 取得済みの動画 ID とタイトルで 05 の API を呼び、登録成功後にメモリ上の条件・キャッシュ・画面を更新する。トグル OFF 時の扱いは 01 の契約に従う
    - 動画へのリンク遷移を誤発火させず、二重送信を防ぐ。API 失敗時は登録成功として扱わず、再試行できる状態を維持する
    - 完了条件 : マウス・タッチで登録でき、同じ動画の別カードにも反映される。重複登録・通信失敗・401 を確認する
- [ ] 21. サムネイル上の「このチャンネルを非表示にする」ボタンを実装する
    - 対象 : メインスクリプトのカード操作 UI・チャンネル登録処理
    - DOM から取れたハンドルまたはチャンネル ID と名前で 06 の API を呼び、両方確認できた場合は同一レコードに保持する
    - 識別子が取れないカードでは誤登録できないようにし、表示名から識別子を推測しない。両識別子の紐付けは確認できた情報だけで行う
    - 完了条件 : 片方だけ・両方・識別子なし、既存情報の補完・競合、失敗時の再試行を確認し、成功後は同じチャンネルのカードとキャッシュへ反映する

### 5. 導入・仕上げ

- [ ] 22. Tampermonkey と iPhone ブックマークレットの導入手順を完成させる
    - 対象 : `README.md` の導入へのリンク、適切な機能文書、必要なスクリプト例
    - PC の Tampermonkey と iPhone のブックマークレットから同じ `/ytf.js` を読み込む最終コード、API 接続先とトークンの設定・更新方法を記載する
    - 既存の Trusted Types を使う読込例について、対応有無・ポリシーの重複作成・再実行・取得失敗を考慮する。トークンを URL・ログ・公開コードへ含めない
    - 完了条件 : PC Brave、iPhone Safari・Brave での導入・再実行手順が揃う。実機未確認の環境は確認済みと区別して記録する
- [ ] 23. サンプル機能を整理し、現行仕様と文書索引を更新する
    - 対象 : README に列挙されたサンプル、`server/routes/api/api.ts` の `/test`、SQL、README・Architecture・機能/API 文書
    - 呼び出し元がないことを確認して不要なサンプルを削除する。既存コメントの変更・削除は作業ルールに従い着手範囲として確認する
    - 実装済みのページ・API・認証方式・DB・スクリプト配信とキャッシュの仕様を文書へ反映し、説明の所有先にリンクする。README の JWT ログイン記載は 01 の決定と実装に合わせる
    - 完了条件 : サンプルへの不要な参照や仮のアラートが残らず、導入・保守に必要な情報へ文書索引から到達できる
- [ ] 24. コンセプト全体の結合・実機検証を完了する
    - 引継ぎ : 17で未確認の PC/モバイルのホーム動画・関連動画欄の Shorts、18・19の非表示と復元・デバッグ表示を実機でまとめて確認する。実装を先に進める方針により、これらの未検証を合格扱いにはしていない
    - PC Brave の Tampermonkey、iPhone Safari・Brave のブックマークレットについて、実際に利用できる `www.youtube.com`・`m.youtube.com` のホーム・関連動画・検索結果を確認する。リダイレクト等で到達できない組み合わせも記録する
    - 4リソースの管理 CRUD → API → メインスクリプトへの反映、動画・チャンネルのボタン登録 → D1 → 管理画面への反映を確認する
    - ページ別の初期 ON/OFF、復元、SPA 遷移、追加読込、重複ロード、キャッシュ更新、通信断・認証失敗・不正パターンからの復旧を確認する
    - 本番で必要な DB 適用・シークレット登録・デプロイは開発者へ手順を渡して実施結果を受け取る。AI が実行できない実機操作も開発者へ具体的な確認手順を渡す
    - 完了条件 : 環境ごとの検証結果と既知の制限を記録し、未確認を合格扱いにしない。不具合があれば修正タスクをこの項目の前に追加して開発者確認を取り、再検証後に完了とする
